@model IEnumerable<EntityLayer.Concrete.Product>

<main>
    <section class="w3l-witemshny-main py-5">
        <div class="container py-md-4">
            <!--/row-1-->
            @foreach (var item in Model)
            {
                <div class="witemshny-grids row">
                    <div class="col-xl-2 col-md-4 col-6 product-incfhny mt-4">
                        <div class="weitemshny-grid oposition-relative">
                            <a href="#img" class="d-block zoom"><img src="@item.ProductImage" alt="" class="img-fluid news-image"></a>
                            <div class="witemshny-inf">
                            </div>
                        </div>
                        <h4 class="gdnhy-1 mt-4">
                            <a href="#img">@item.ProductName</a>
                        </h4>
                    </div>
                </div>
            }
        </div>
    </section>
</main>
